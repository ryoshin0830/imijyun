# React Native/Expoとの互換性のため、hoisted node-linkerを使用
node-linker=hoisted

# パッケージのホイスト設定
hoist-pattern[]=*

# シンボリックリンクの無効化（React Nativeツールチェイン互換性のため）
node-linker=hoisted

# Expoプロジェクトでの互換性向上
auto-install-peers=true
strict-peer-dependencies=false

# パフォーマンス向上設定
prefer-frozen-lockfile=true